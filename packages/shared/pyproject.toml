[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "reviva-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic-settings>=2.7.0", "sqlmodel>=0.0.22"]

[tool.setuptools.packages.find]
include = ["reviva_shared*"]
